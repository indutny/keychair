#!/usr/bin/env node
var keychair = require('..');
var fs = require('fs');

var chain = fs.readFileSync(process.argv[2]);
var out = keychair.parse(chain);
return;
console.log(require('util').inspect(out, false, 300));
